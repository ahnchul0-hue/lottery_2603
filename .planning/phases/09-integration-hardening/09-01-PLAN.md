---
phase: 09-integration-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/api/routes.py
  - backend/app/schemas/reflection.py
  - backend/tests/test_integration.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "GET /api/data?machine=4호기 returns 400 with Korean error message containing '유효하지 않은'"
    - "POST /api/predict with invalid strategy returns 422 with Pydantic validation detail"
    - "POST /api/predict with empty body returns 422"
    - "GET /api/statistics/heatmap returns 200 with exactly 3 rows"
    - "POST /api/reflect with missing API key returns 503 with Korean message"
    - "POST /api/reflect with invalid machine string still processes (no Literal gate)"
    - "All 5 endpoints respond correctly on happy path in a single test session"
  artifacts:
    - path: "backend/tests/test_integration.py"
      provides: "Comprehensive integration test suite for all 5 endpoints"
      min_lines: 80
    - path: "backend/app/api/routes.py"
      provides: "Hardened error handling with Korean messages"
      contains: "유효하지 않은"
  key_links:
    - from: "backend/tests/test_integration.py"
      to: "backend/app/api/routes.py"
      via: "httpx AsyncClient through conftest.py fixture"
      pattern: "client\\.get|client\\.post"
---

<objective>
Harden backend error handling with Korean error messages and create comprehensive integration test suite covering all 5 API endpoints (happy path + error paths).

Purpose: Ensure backend returns meaningful error responses (not generic 500s) for invalid inputs, and verify the entire API surface with automated tests.
Output: Hardened routes.py with Korean error messages + test_integration.py with full endpoint coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integration-hardening/09-CONTEXT.md

@backend/app/api/routes.py
@backend/app/schemas/lottery.py
@backend/app/schemas/reflection.py
@backend/app/schemas/statistics.py
@backend/app/main.py
@backend/app/config.py
@backend/app/strategies/__init__.py
@backend/app/services/data_loader.py
@backend/app/services/reflection_service.py
@backend/tests/conftest.py
@backend/tests/test_api.py

<interfaces>
<!-- Key types and contracts the executor needs -->

From backend/app/schemas/lottery.py:
```python
class PredictRequest(BaseModel):
    machine: Literal["1호기", "2호기", "3호기"]
    strategy: Literal["frequency", "pattern", "range", "balance", "composite"]

class MachineDataResponse(BaseModel):
    machine: str
    total_draws: int
    draws: list[LotteryDraw]
```

From backend/app/schemas/reflection.py:
```python
class ReflectRequest(BaseModel):
    machine: str  # NOTE: NOT Literal-gated, accepts any string
    round_number: int
    comparison_data: dict
    past_reflections: list[str] | None = None
```

From backend/app/schemas/statistics.py:
```python
class HeatmapResponse(BaseModel):
    rows: list[HeatmapRow]  # exactly 3 rows
```

From backend/tests/conftest.py:
```python
@pytest.fixture
async def client():
    async with lifespan(app):
        transport = ASGITransport(app=app)
        async with AsyncClient(transport=transport, base_url="http://test") as ac:
            yield ac
```

From backend/app/services/data_loader.py:
```python
def get_draws_for_machine(self, machine: str) -> list[LotteryDraw]:
    if machine not in self._by_machine:
        raise ValueError("Unknown machine: {machine}. Valid: 1호기, 2호기, 3호기")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden backend error responses with Korean messages</name>
  <files>backend/app/api/routes.py, backend/app/schemas/reflection.py</files>
  <read_first>
    - backend/app/api/routes.py (current error handling)
    - backend/app/schemas/reflection.py (ReflectRequest schema)
    - backend/app/schemas/lottery.py (PredictRequest Literal validation)
    - backend/app/services/data_loader.py (get_draws_for_machine ValueError message)
    - backend/app/services/reflection_service.py (ValueError for missing key)
  </read_first>
  <action>
    Per D-01: Strengthen input validation on existing 5 endpoints with clear Korean error messages.
    Per D-02: Keep per-endpoint validation (no global exception handler). Use existing FastAPI HTTPException pattern.

    In `backend/app/api/routes.py`, update error messages to Korean:

    1. `get_machine_data` endpoint: Change the ValueError catch to return Korean message.
       Replace the existing `raise HTTPException(status_code=400, detail=str(e))` with:
       `raise HTTPException(status_code=400, detail=f"유효하지 않은 호기입니다: {machine}. 사용 가능: 1호기, 2호기, 3호기")`

    2. `predict_numbers` endpoint:
       - Machine ValueError catch: `raise HTTPException(status_code=400, detail=f"유효하지 않은 호기입니다: {request.machine}. 사용 가능: 1호기, 2호기, 3호기")`
       - Strategy KeyError catch: `raise HTTPException(status_code=400, detail=f"유효하지 않은 전략입니다: {request.strategy}. 사용 가능: frequency, pattern, range, balance, composite")`
       - Data not loaded 503: `raise HTTPException(status_code=503, detail="데이터가 아직 로드되지 않았습니다")`
       - Decay engine not initialized 503: `raise HTTPException(status_code=503, detail="감쇠 엔진이 초기화되지 않았습니다")`

    3. `get_heatmap_data` endpoint:
       - Data not loaded 503: `raise HTTPException(status_code=503, detail="데이터가 아직 로드되지 않았습니다")`

    4. `create_reflection` endpoint:
       - API key missing 503: `raise HTTPException(status_code=503, detail="AI 반성 기능을 사용할 수 없습니다 (API 키 미설정)")`
       - API call failure 502: `raise HTTPException(status_code=502, detail=f"AI 반성 생성에 실패했습니다: {str(e)}")`

    5. `health_check` endpoint: No changes needed (health check is internal/diagnostic).

    Do NOT add Literal validation to ReflectRequest.machine -- per CONTEXT, the reflect endpoint should accept any machine string since it just passes it to the AI prompt. Leave ReflectRequest schema unchanged.

    IMPORTANT: The ValueError catch blocks in get_machine_data and predict_numbers currently use `str(e)` -- replace with explicit Korean messages so error responses are user-friendly regardless of internal exception message language.
  </action>
  <verify>
    <automated>cd /Users/ahnchul0/Downloads/Lottery_2603/backend && uv run python -c "from app.api.routes import router; print('routes import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - routes.py contains "유효하지 않은 호기입니다" in get_machine_data error path
    - routes.py contains "유효하지 않은 전략입니다" in predict_numbers KeyError path
    - routes.py contains "데이터가 아직 로드되지 않았습니다" for 503 data-not-loaded
    - routes.py contains "AI 반성 기능을 사용할 수 없습니다" for reflect 503
    - routes.py contains "AI 반성 생성에 실패했습니다" for reflect 502
    - ReflectRequest.machine remains `str` type (NOT changed to Literal)
  </acceptance_criteria>
  <done>All 5 endpoints return Korean error messages for invalid inputs. Error responses are user-friendly.</done>
</task>

<task type="auto">
  <name>Task 2: Create comprehensive integration test suite</name>
  <files>backend/tests/test_integration.py</files>
  <read_first>
    - backend/tests/test_api.py (existing tests to avoid duplication)
    - backend/tests/conftest.py (client fixture)
    - backend/app/api/routes.py (updated error messages from Task 1)
    - backend/app/config.py (ANTHROPIC_API_KEY setting)
  </read_first>
  <action>
    Per D-04: pytest + httpx integration tests for all 5 endpoints.
    Per D-05: Cover full user flow at API level.

    Create `backend/tests/test_integration.py` with the following test structure.
    All tests use the `client` fixture from conftest.py (async with @pytest.mark.anyio).

    Test structure -- each test function is a focused assertion:

    **Section 1: GET /api/health (서버 상태 확인)**
    - `test_health_returns_200_with_loaded_data`: GET /api/health -> 200, status="ok", data_loaded=True, total_records=417

    **Section 2: GET /api/data (호기별 데이터 조회)**
    - `test_data_valid_machine_1`: GET /api/data?machine=1호기 -> 200, machine="1호기", total_draws=134, draws is list
    - `test_data_valid_machine_2`: GET /api/data?machine=2호기 -> 200, total_draws > 0
    - `test_data_valid_machine_3`: GET /api/data?machine=3호기 -> 200, total_draws > 0
    - `test_data_invalid_machine_returns_400`: GET /api/data?machine=4호기 -> 400, detail contains "유효하지 않은 호기"
    - `test_data_empty_machine_returns_400`: GET /api/data?machine= (empty string) -> 400, detail contains "유효하지 않은 호기"
    - `test_data_missing_param_returns_422`: GET /api/data (no param) -> 422

    **Section 3: POST /api/predict (예측 - 유효/무효 입력)**
    - `test_predict_all_strategies_succeed`: parametrize over all 5 strategies with machine=1호기, each returns 200 with 5 games of 6 sorted unique numbers in [1,45]
    - `test_predict_all_machines_succeed`: parametrize over 3 machines with strategy=frequency, each returns 200
    - `test_predict_invalid_machine_returns_422`: POST with machine="4호기" -> 422 (Pydantic Literal rejects)
    - `test_predict_invalid_strategy_returns_422`: POST with strategy="nonexistent" -> 422 (Pydantic Literal rejects)
    - `test_predict_empty_body_returns_422`: POST with no body -> 422
    - `test_predict_partial_body_returns_422`: POST with {"machine": "1호기"} (missing strategy) -> 422
    - `test_predict_games_are_valid`: POST with machine=1호기, strategy=frequency -> verify each game: len==6, all in [1,45], sorted, unique

    **Section 4: GET /api/statistics/heatmap (히트맵)**
    - `test_heatmap_returns_3_rows`: GET /api/statistics/heatmap -> 200, len(rows)==3
    - `test_heatmap_row_structure`: Each row has machine (string), deviations (dict with 45 keys "1"-"45"), total_draws (int > 0)
    - `test_heatmap_machines_complete`: The 3 rows cover exactly {"1호기", "2호기", "3호기"}

    **Section 5: POST /api/reflect (AI 반성 - API 키 없을 때 503)**
    - `test_reflect_without_api_key_returns_503`: POST /api/reflect with valid body but no ANTHROPIC_API_KEY -> 503, detail contains "API 키" or "사용할 수 없습니다"
      Implementation: Temporarily set settings.ANTHROPIC_API_KEY = None before request, restore after.
      Use `monkeypatch.setattr("app.config.settings", "ANTHROPIC_API_KEY", None)` or directly mutate the settings object.
    - `test_reflect_missing_body_returns_422`: POST /api/reflect with no body -> 422

    **Section 6: Full user flow simulation (전체 사용자 플로우)**
    - `test_full_user_flow_machine_1`: Sequential: health check -> get data for 1호기 -> predict frequency -> predict all 5 strategies -> heatmap -> all pass 200
    - `test_full_flow_all_machines`: For each of 1호기, 2호기, 3호기: get data -> predict all 5 strategies -> all return 200 with valid games

    Each test function should have a clear docstring in Korean describing what it validates.

    IMPORTANT: Do NOT duplicate tests from test_api.py. The test_integration.py focuses on:
    - Korean error message validation (testing Task 1's changes)
    - Heatmap endpoint coverage (not tested in test_api.py)
    - Reflect endpoint coverage (not tested in test_api.py)
    - Full flow sequential tests (not in test_api.py)
    - Edge cases like empty string machine, partial body
  </action>
  <verify>
    <automated>cd /Users/ahnchul0/Downloads/Lottery_2603/backend && uv run pytest tests/test_integration.py -v --tb=short 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - test_integration.py exists with at least 15 test functions
    - All tests in test_integration.py pass (exit code 0)
    - test_data_invalid_machine_returns_400 asserts "유효하지 않은 호기" in response detail
    - test_heatmap_returns_3_rows asserts len(rows)==3
    - test_reflect_without_api_key_returns_503 asserts status_code==503
    - test_full_user_flow_machine_1 calls health + data + predict + heatmap sequentially
    - Existing tests in test_api.py still pass (no regressions)
  </acceptance_criteria>
  <done>Comprehensive integration test suite passes for all 5 endpoints covering happy path and error paths with Korean error message verification.</done>
</task>

</tasks>

<verification>
1. `cd /Users/ahnchul0/Downloads/Lottery_2603/backend && uv run pytest tests/ -v --tb=short` -- ALL tests pass (both test_api.py and test_integration.py)
2. `grep -c "유효하지 않은" backend/app/api/routes.py` -- at least 2 occurrences (machine + strategy)
3. `grep -c "def test_" backend/tests/test_integration.py` -- at least 15 test functions
</verification>

<success_criteria>
- Backend returns Korean error messages for all invalid inputs (not English/generic errors)
- Integration tests cover all 5 endpoints (health, data, predict, heatmap, reflect)
- Both happy path and error path tested for each endpoint
- Full user flow test passes sequentially (health -> data -> predict -> heatmap)
- Zero test regressions in existing test_api.py
</success_criteria>

<output>
After completion, create `.planning/phases/09-integration-hardening/09-01-SUMMARY.md`
</output>
