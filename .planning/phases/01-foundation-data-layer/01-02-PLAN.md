---
phase: 01-foundation-data-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/vite-env.d.ts
autonomous: true
requirements:
  - INFRA-02

must_haves:
  truths:
    - "React (Vite) frontend starts on localhost:5173 and renders a page in the browser"
    - "Tailwind CSS v4 utility classes are functional (bg-*, text-*, flex, etc.)"
    - "Design tokens (CSS custom properties) are declared in index.css for future dark mode"
  artifacts:
    - path: "frontend/package.json"
      provides: "Node.js project with react, vite, tailwindcss dependencies"
      contains: "tailwindcss"
    - path: "frontend/vite.config.ts"
      provides: "Vite config with React and Tailwind plugins"
      contains: "@tailwindcss/vite"
    - path: "frontend/src/index.css"
      provides: "Tailwind import and design token CSS custom properties"
      contains: "@import"
    - path: "frontend/src/App.tsx"
      provides: "Placeholder App component with Tailwind classes"
      contains: "min-h-screen"
  key_links:
    - from: "frontend/vite.config.ts"
      to: "frontend/src/index.css"
      via: "Vite processes CSS through tailwindcss plugin"
      pattern: "tailwindcss"
    - from: "frontend/src/main.tsx"
      to: "frontend/src/App.tsx"
      via: "renders App component into DOM"
      pattern: "App"
---

<objective>
Scaffold the React + Vite + TypeScript + Tailwind CSS v4 frontend with design tokens from the UI-SPEC.

Purpose: Establish the frontend foundation with correct tooling and design system tokens so Phase 5+ UI work inherits consistent colors, typography, and spacing.
Output: A running Vite dev server rendering a placeholder page with functional Tailwind styling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-RESEARCH.md
@.planning/phases/01-foundation-data-layer/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite React TypeScript project with Tailwind CSS v4</name>
  <files>
    frontend/package.json,
    frontend/vite.config.ts,
    frontend/tsconfig.json,
    frontend/tsconfig.app.json,
    frontend/tsconfig.node.json,
    frontend/index.html,
    frontend/src/main.tsx,
    frontend/src/vite-env.d.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-data-layer/01-RESEARCH.md,
    .planning/phases/01-foundation-data-layer/01-UI-SPEC.md
  </read_first>
  <action>
    **Step 1: Create Vite project (per RESEARCH.md)**
    ```bash
    cd /Users/ahnchul0/Downloads/Lottery_2603
    npm create vite@latest frontend -- --template react-ts
    cd frontend
    npm install
    ```

    **Step 2: Install Tailwind CSS v4 (per RESEARCH.md Pattern 5)**
    ```bash
    npm install tailwindcss @tailwindcss/vite
    ```

    **Step 3: Update `frontend/vite.config.ts`**
    Replace the generated config with:
    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react'
    import tailwindcss from '@tailwindcss/vite'

    export default defineConfig({
      plugins: [
        react(),
        tailwindcss(),
      ],
    })
    ```
    This is exactly the Tailwind v4 + Vite pattern from RESEARCH.md. No PostCSS config, no tailwind.config.js (Tailwind v4 does not use them).

    **Step 4: Remove Vite scaffold cruft**
    Delete files that are not needed:
    - `frontend/src/App.css` (will use Tailwind utility classes instead)
    - `frontend/src/assets/` directory (no static assets in Phase 1)
    - `frontend/public/vite.svg` (not needed)

    Do NOT delete: `frontend/src/vite-env.d.ts` (needed for TypeScript Vite types).

    **Step 5: Verify Vite template generated expected files**
    Confirm these files exist after scaffolding: `package.json`, `vite.config.ts`, `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`, `index.html`, `src/main.tsx`.
  </action>
  <verify>
    <automated>cd /Users/ahnchul0/Downloads/Lottery_2603/frontend && node -e "const pkg = require('./package.json'); console.log('react:', pkg.dependencies.react); console.log('tailwindcss:', pkg.dependencies.tailwindcss); console.log('vite-tw:', pkg.dependencies['@tailwindcss/vite'])"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` contains `"react"` in dependencies
    - `frontend/package.json` contains `"tailwindcss"` in dependencies
    - `frontend/package.json` contains `"@tailwindcss/vite"` in dependencies
    - `frontend/package.json` contains `"typescript"` in devDependencies
    - `frontend/vite.config.ts` contains `import tailwindcss from '@tailwindcss/vite'`
    - `frontend/vite.config.ts` contains `tailwindcss()` in plugins array
    - `frontend/vite.config.ts` contains `react()` in plugins array
    - `frontend/tsconfig.json` exists
    - `frontend/index.html` exists and contains `<div id="root">`
    - `frontend/src/main.tsx` exists and contains `createRoot`
    - `frontend/node_modules/` directory exists (npm install completed)
  </acceptance_criteria>
  <done>
    Vite React TypeScript project scaffolded with Tailwind CSS v4 plugin configured. All template files in place, dependencies installed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create design tokens in index.css and placeholder App.tsx</name>
  <files>
    frontend/src/index.css,
    frontend/src/App.tsx,
    frontend/src/main.tsx
  </files>
  <read_first>
    frontend/src/main.tsx,
    frontend/vite.config.ts,
    .planning/phases/01-foundation-data-layer/01-UI-SPEC.md
  </read_first>
  <action>
    **Step 1: Create `frontend/src/index.css` with Tailwind import and design tokens**
    This is the CSS foundation for the entire project. Per UI-SPEC.md "CSS Custom Properties Foundation":

    ```css
    @import "tailwindcss";

    @theme {
      --color-surface: #f8fafc;
      --color-card: #ffffff;
      --color-accent: #3b82f6;
      --color-destructive: #ef4444;
      --color-text-primary: #0f172a;
      --color-text-secondary: #64748b;
      --color-border: #e2e8f0;
      --color-success: #22c55e;
    }
    ```

    NOTE: Dark mode values (slate-900, slate-800, etc.) are documented in UI-SPEC.md but NOT implemented yet -- that is Phase 8 (UI-03). Only light mode tokens are active in Phase 1.

    **Step 2: Update `frontend/src/main.tsx`**
    Ensure it imports `index.css`:
    ```tsx
    import { StrictMode } from 'react'
    import { createRoot } from 'react-dom/client'
    import './index.css'
    import App from './App'

    createRoot(document.getElementById('root')!).render(
      <StrictMode>
        <App />
      </StrictMode>,
    )
    ```

    **Step 3: Create `frontend/src/App.tsx` placeholder page**
    Per UI-SPEC.md layout specs and copywriting contract, create a centered card page.
    The page does NOT connect to the backend yet (that is Plan 03). It shows static placeholder content.

    Layout requirements from UI-SPEC.md:
    - Full viewport height: `min-h-screen`
    - Background: `bg-surface` (uses --color-surface token)
    - Centered both axes: `flex items-center justify-center`
    - Card: max-width 400px (`max-w-[400px] w-full mx-4`), padding 32px (`p-8`), border-radius 12px (`rounded-xl`), shadow (`shadow-md`)
    - Card border: `border border-border` (uses --color-border token)
    - Card background: `bg-card` (uses --color-card token)
    - Heading: `text-2xl font-bold leading-tight text-text-primary` with text "Lottery Predictor"
    - Body text: `text-text-secondary` with text "Connecting to backend..."

    ```tsx
    function App() {
      return (
        <div className="min-h-screen bg-surface flex items-center justify-center">
          <div className="max-w-[400px] w-full mx-4 bg-card p-8 rounded-xl shadow-md border border-border text-center">
            <h1 className="text-2xl font-bold leading-tight text-text-primary mb-4">
              Lottery Predictor
            </h1>
            <p className="text-text-secondary">Connecting to backend...</p>
          </div>
        </div>
      )
    }

    export default App
    ```

    This is a static placeholder. Plan 03 will add the actual health check fetch and dynamic states (connected, error).
  </action>
  <verify>
    <automated>cd /Users/ahnchul0/Downloads/Lottery_2603/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/index.css` first line is `@import "tailwindcss";`
    - `frontend/src/index.css` contains `@theme {` block
    - `frontend/src/index.css` contains `--color-surface: #f8fafc`
    - `frontend/src/index.css` contains `--color-card: #ffffff`
    - `frontend/src/index.css` contains `--color-accent: #3b82f6`
    - `frontend/src/index.css` contains `--color-destructive: #ef4444`
    - `frontend/src/index.css` contains `--color-text-primary: #0f172a`
    - `frontend/src/index.css` contains `--color-text-secondary: #64748b`
    - `frontend/src/index.css` contains `--color-border: #e2e8f0`
    - `frontend/src/index.css` contains `--color-success: #22c55e`
    - `frontend/src/App.tsx` contains `min-h-screen` and `bg-surface`
    - `frontend/src/App.tsx` contains `Lottery Predictor`
    - `frontend/src/App.tsx` contains `Connecting to backend...`
    - `frontend/src/main.tsx` contains `import './index.css'`
    - `npx tsc --noEmit` exits with 0 errors (TypeScript compiles cleanly)
  </acceptance_criteria>
  <done>
    Frontend renders a placeholder card page at localhost:5173 using Tailwind CSS v4 utility classes. Design tokens declared as CSS custom properties in @theme block. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
1. `cd /Users/ahnchul0/Downloads/Lottery_2603/frontend && npm run dev` -- Vite starts on port 5173
2. Open `http://localhost:5173` -- shows centered card with "Lottery Predictor" heading and "Connecting to backend..." text
3. Card has visible shadow, rounded corners, and light gray background (Tailwind classes working)
4. `npx tsc --noEmit` -- zero TypeScript errors
</verification>

<success_criteria>
- React (Vite) frontend starts on localhost:5173 and renders a page (INFRA-02)
- Tailwind CSS v4 utility classes produce visible styling (background colors, shadows, text sizes)
- Design tokens from UI-SPEC.md declared as CSS custom properties in @theme block
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-layer/01-02-SUMMARY.md`
</output>
