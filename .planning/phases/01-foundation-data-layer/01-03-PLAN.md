---
phase: 01-foundation-data-layer
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - frontend/src/App.tsx
autonomous: false
requirements:
  - INFRA-03

must_haves:
  truths:
    - "Frontend at localhost:5173 successfully fetches /api/health from backend at localhost:8000 without CORS errors"
    - "Connected state shows green dot, 'Status: Connected', and 'Data loaded: 417 records'"
    - "Error state shows red error heading and troubleshooting instruction when backend is not running"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Placeholder page with live health check against backend"
      contains: "fetch"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "http://localhost:8000/api/health"
      via: "fetch() in useEffect on mount"
      pattern: "fetch.*localhost:8000"
---

<objective>
Wire the frontend placeholder page to call the backend health endpoint, proving CORS works end-to-end.

Purpose: Validate that the frontend and backend can communicate across origins -- the cross-cutting INFRA-03 requirement. This is the integration proof for the entire Phase 1 foundation.
Output: A placeholder page that displays live backend connection status with three visual states (loading, connected, error).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-layer/01-CONTEXT.md
@.planning/phases/01-foundation-data-layer/01-UI-SPEC.md

<interfaces>
<!-- From Plan 01: backend API contract -->
GET /api/health returns:
```json
{
  "status": "ok",
  "data_loaded": true,
  "total_records": 417
}
```

<!-- From Plan 02: frontend design tokens available as Tailwind colors -->
CSS custom properties in @theme:
  --color-surface: #f8fafc     (bg-surface)
  --color-card: #ffffff         (bg-card)
  --color-accent: #3b82f6      (text-accent)
  --color-destructive: #ef4444 (text-destructive)
  --color-text-primary: #0f172a (text-text-primary)
  --color-text-secondary: #64748b (text-text-secondary)
  --color-border: #e2e8f0      (border-border)
  --color-success: #22c55e     (text-success, bg-success)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add health check fetch and three visual states to App.tsx</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    frontend/src/App.tsx,
    frontend/src/index.css,
    .planning/phases/01-foundation-data-layer/01-UI-SPEC.md
  </read_first>
  <action>
    Update `frontend/src/App.tsx` to add a `useEffect` health check and three display states.

    **API constant:**
    ```typescript
    const API_BASE = 'http://localhost:8000/api'
    ```

    **State variables:**
    ```typescript
    const [health, setHealth] = useState<{
      status: string
      data_loaded: boolean
      total_records: number
    } | null>(null)
    const [error, setError] = useState<string | null>(null)
    ```

    **useEffect on mount:**
    ```typescript
    useEffect(() => {
      fetch(`${API_BASE}/health`)
        .then((res) => {
          if (!res.ok) throw new Error(`HTTP ${res.status}`)
          return res.json()
        })
        .then(setHealth)
        .catch((err) => setError(err.message))
    }, [])
    ```

    **Three visual states per UI-SPEC.md copywriting contract:**

    1. **Loading state** (health === null AND error === null):
       - Text: "Connecting to backend..." in `text-text-secondary`

    2. **Success state** (health !== null):
       - Green dot: `<span className="inline-block w-2 h-2 rounded-full bg-success mr-2"></span>` before "Connected"
       - Line 1: "Status: Connected" in `text-text-primary`
       - Line 2: "Data loaded: {health.total_records} records" in `text-text-primary`
       - Use exact copywriting from UI-SPEC: `Status: Connected` and `Data loaded: {total_records} records`

    3. **Error state** (error !== null):
       - Heading: "Backend Connection Failed" in `text-destructive font-bold`
       - Body: "Could not reach the backend server. Make sure FastAPI is running on port 8000." in `text-text-secondary`

    **Full layout (same card structure from Plan 02, now with dynamic content):**
    - `min-h-screen bg-surface flex items-center justify-center`
    - Card: `max-w-[400px] w-full mx-4 bg-card p-8 rounded-xl shadow-md border border-border`
    - Heading: "Lottery Predictor" in `text-2xl font-bold leading-tight text-text-primary mb-6`
    - Content area below heading with `space-y-3 text-left`

    Imports needed: `import { useEffect, useState } from 'react'`
  </action>
  <verify>
    <automated>cd /Users/ahnchul0/Downloads/Lottery_2603/frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/App.tsx` contains `const API_BASE = 'http://localhost:8000/api'`
    - `frontend/src/App.tsx` contains `fetch(\`\${API_BASE}/health\`)`
    - `frontend/src/App.tsx` contains `useEffect` with empty dependency array `[]`
    - `frontend/src/App.tsx` contains `useState` for `health` and `error`
    - `frontend/src/App.tsx` contains the text `Status: Connected`
    - `frontend/src/App.tsx` contains the text `Data loaded:`
    - `frontend/src/App.tsx` contains the text `Backend Connection Failed`
    - `frontend/src/App.tsx` contains the text `Make sure FastAPI is running on port 8000`
    - `frontend/src/App.tsx` contains `bg-success` (green dot class)
    - `frontend/src/App.tsx` contains `text-destructive` (error text class)
    - `npx tsc --noEmit` exits with 0 errors
  </acceptance_criteria>
  <done>
    App.tsx fetches /api/health on mount and displays one of three states. TypeScript compiles without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end CORS connectivity</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Human verifies the complete end-to-end integration between frontend (localhost:5173) and backend (localhost:8000).

    **What was built:** Frontend placeholder page that calls backend health endpoint across origins with three visual states (loading, connected with green dot and record count, error with troubleshooting message).

    **How to verify:**
    1. Start backend: open a terminal, run `cd /Users/ahnchul0/Downloads/Lottery_2603/backend && uv run fastapi dev app/main.py --port 8000`
    2. Start frontend: open another terminal, run `cd /Users/ahnchul0/Downloads/Lottery_2603/frontend && npm run dev`
    3. Open http://localhost:5173 in browser
    4. Expected: centered card shows "Lottery Predictor" heading, green dot next to "Status: Connected", and "Data loaded: 417 records"
    5. No CORS errors in browser DevTools console (open with F12 -> Console tab)
    6. Stop the backend (Ctrl+C), then refresh the page
    7. Expected: card shows "Backend Connection Failed" in red and troubleshooting instruction text

    **Resume signal:** Type "approved" if both connected and error states display correctly with no CORS errors. Otherwise describe any issues.
  </action>
  <verify>
    <automated>cd /Users/ahnchul0/Downloads/Lottery_2603/frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Browser at http://localhost:5173 shows "Status: Connected" when backend is running
    - Browser at http://localhost:5173 shows "Backend Connection Failed" when backend is stopped
    - No CORS errors visible in browser DevTools console
    - User types "approved"
  </acceptance_criteria>
  <done>
    End-to-end CORS connectivity verified by human. Frontend communicates with backend across origins without errors. Phase 1 foundation complete.
  </done>
</task>

</tasks>

<verification>
1. Both servers running simultaneously -- frontend on 5173, backend on 8000
2. Browser shows "Status: Connected" and "Data loaded: 417 records" with green indicator
3. Browser DevTools Console shows no CORS errors (no "Access-Control-Allow-Origin" errors)
4. Stopping backend and refreshing shows error state with "Backend Connection Failed"
5. `npx tsc --noEmit` -- zero TypeScript errors
</verification>

<success_criteria>
- Frontend successfully calls backend /api/health across origins (INFRA-03)
- No CORS errors in browser console
- Three visual states work correctly (loading, connected, error)
- The complete Phase 1 foundation is proven: backend loads data, frontend renders, cross-origin communication works
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-layer/01-03-SUMMARY.md`
</output>
